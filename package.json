{
  "name": "poligraph-mcp",
  "version": "2.0.0",
  "description": "Serveur MCP pour accéder aux données de Poligraph — observatoire citoyen de la vie politique",
  "type": "module",
  "bin": {
    "poligraph-mcp": "build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "inspect": "npx @modelcontextprotocol/inspector build/index.js",
    "start:http": "node build/http.js",
    "dev:http": "tsc && node build/http.js",
    "test": "node --test build/tests/api-contract.test.js",
    "test:build": "tsc && node --test build/tests/api-contract.test.js"
  },
  "keywords": [
    "mcp",
    "poligraph",
    "politique",
    "france",
    "claude"
  ],
  "author": "ironlam",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^20.17.0",
    "@vercel/node": "^5.6.3",
    "typescript": "^5.7.0"
  }
}
